This is a React + TypeScript codebase called Temantiket. Make these mobile UX improvements:

---

FIX 1: Group "Lainnya" menu in src/components/DashboardLayout.tsx

The mobile bottom sheet "Menu Lengkap" currently renders moreItems as a flat grid with no grouping. Find the grid section that maps over moreItems (it renders buttons with icons in a grid-cols-4 layout).

Replace the flat map with grouped sections. Add a type for groups and restructure OWNER_MORE_ITEMS in src/config/navMenu.ts by replacing the flat array with a grouped structure. Add a new export:

export const OWNER_MORE_GROUPS = [
  { label: "Alat Bantu", items: [CALC, ITINERARY, TICKETS, FLIGHT_SEARCH] },
  { label: "Keuangan",   items: [REPORTS, EXPORTS] },
  { label: "Pemasaran",  items: [BC, CAPTION, NOTES] },
  { label: "Tim & Agen", items: [VISA_TRACKER, AGENT_CENTER, LEADERBOARD, STAFF_MGMT, AUDIT] },
  { label: "Lainnya",    items: [SETTINGS] },
];

Keep OWNER_MORE_ITEMS as-is for backward compatibility (it's used elsewhere). Add OWNER_MORE_GROUPS as a new export.

In DashboardLayout.tsx, import OWNER_MORE_GROUPS and for owner role, render the grouped layout instead of the flat grid. Each group should have a small section label above its grid row. Use this pattern:
  {isOwner
    ? OWNER_MORE_GROUPS.map(group => (
        <div key={group.label}>
          <p className="text-[10px] font-bold uppercase tracking-wider text-muted-foreground px-1 mb-2 mt-3">
            {group.label}
          </p>
          <div className="grid grid-cols-4 gap-x-2 gap-y-3">
            {group.items.map(item => /* existing button JSX */ )}
          </div>
        </div>
      ))
    : /* existing flat grid for agent/staff */
  }

---

FIX 2: Allow agents to access Caption Generator

In src/App.tsx, find this route:
  path="/caption-generator"
  RequireRole roles={["owner"]}

Change roles to: ["owner", "agent"]

In src/config/navMenu.ts, find:
  const CAPTION: NavItemConfig = { ..., ownerOnly: true }

Remove ownerOnly: true from CAPTION.

In AGENT_SIDEBAR_SECTIONS, add CAPTION to the "pemasaran" section items array alongside BC and NOTES.

In AGENT_MORE_ITEMS array, add m(CAPTION) after m(BC).

---

FIX 3: Add empty states to list pages

In src/pages/Orders.tsx, find where the order list is rendered (the map over filtered orders). Add a condition before the list: if filtered orders length is 0 and not loading, show:
  <div className="flex flex-col items-center justify-center py-20 text-center">
    <ShoppingBag className="h-12 w-12 text-muted-foreground/30 mb-4" />
    <p className="text-sm font-medium text-muted-foreground">Belum ada order</p>
    <p className="text-xs text-muted-foreground/60 mt-1">Buat order pertama untuk mulai mencatat transaksi</p>
    <Button className="mt-4" onClick={() => setAddOpen(true)}>
      <Plus className="h-4 w-4 mr-2" /> Buat Order
    </Button>
  </div>

In src/pages/Clients.tsx, same pattern with Users icon and text "Belum ada klien" + button "Tambah Klien" that opens the add client dialog.

ShoppingBag, Plus, Users are already imported in both files.
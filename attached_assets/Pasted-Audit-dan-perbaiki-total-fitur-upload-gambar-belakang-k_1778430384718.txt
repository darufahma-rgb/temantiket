Audit dan perbaiki total fitur upload gambar belakang kartu staff/member/agent sampai benar-benar stabil. Error saat ini masih muncul “Gagal mempersiapkan upload: The page could not be found NOT_FOUND fra1::...”, berarti akar masalah belum selesai dan kemungkinan flow upload masih memakai route/request/page id yang salah, endpoint tidak valid, atau proses prepare upload masih mengarah ke resource yang tidak ada. Jangan tambal sebagian; telusuri dari awal sampai akhir: tombol upload, file picker, fungsi prepare upload, API/endpoint yang dipanggil, Supabase Storage bucket, generate path, upload file, get public/signed URL, update database, fetch ulang data, render kartu, refresh halaman, dan akses lintas device.

Pastikan sistem tidak memakai ID seperti `fra1::...`, `sin1::...`, route slug, temporary object id, blob URL, atau page request id untuk proses database/storage. Gunakan ID database asli yang valid sesuai jenis kartu: `staff.id`, `profile.id`, `user_id`, `agent.id`, atau `member.id`. Audit tabel mana yang benar-benar dipakai untuk staff card, agent card, dan member card, lalu pastikan kolom permanen seperti `card_back_image_url` ada dan bisa di-update. Jika record belum ada, lakukan upsert dengan key yang benar. Pastikan bucket storage ada, permission upload benar, RLS policy mengizinkan owner/admin update semua kartu, dan user biasa hanya update miliknya jika diizinkan.

Tambahkan preflight `/api/health-check` sebelum upload untuk mengecek Supabase URL, service role/admin client, bucket, tabel, kolom, record target, dan permission update. Jika preflight gagal, hentikan upload dan tampilkan error yang jelas. Tambahkan logging detail untuk `cardType`, `tableName`, `recordId`, `userId`, `role`, `bucket`, `storagePath`, `publicUrl`, `updatePayload`, endpoint yang dipanggil, dan error asli. Jangan tampilkan sukses jika upload/database gagal. Jika file sudah terupload tapi database gagal, hapus file tersebut agar tidak menjadi orphan file.

Hasil akhir wajib: upload gambar belakang kartu berhasil tanpa error, URL tersimpan permanen di database, gambar tetap muncul setelah refresh, login ulang, dan dibuka dari device lain. Buat laporan bahasa Indonesia yang mudah dipahami berisi akar masalah, file/flow yang rusak, perbaikan yang dilakukan, dan bukti test berhasil.
